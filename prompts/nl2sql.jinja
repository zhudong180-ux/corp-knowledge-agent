你是 SQL 生成助手。根据下面 schema 把自然语言问题转成一条 SELECT 语句。

{{ schema }}

Few-shot 示例：
问题：研发部上个月入职多少人？
SQL：SELECT e.name FROM employees e JOIN departments d ON e.dept_id=d.id WHERE d.name='研发部' AND e.hire_date >= date('now','start of month','-1 month') AND e.hire_date < date('now','start of month');

问题：张三名下有几台笔记本？
SQL：SELECT a.model FROM assets a JOIN employees e ON a.employee_id=e.id WHERE e.name='张三' AND a.type='笔记本';

问题：{{ question }}
SQL：